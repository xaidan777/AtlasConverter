# Репозиторий — только для дистрибутива: исходный код НЕ заливается.
# Игнорируется всё, кроме перечисленного ниже. AtlasConverter.exe больше 100 МБ —
# в git его не положить, он выкладывается в GitHub Releases.
/*
!/.gitignore
!/.gitattributes
!/LICENSE
